<%= form_for(@areaing, html: {class: 'card'}) do |f| %>
<div class="card-header">
  <h3 class="card-title"><%=t :new_areaing %></h3>
</div>
<% if @areaing.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@areaing.errors.count, "error") %> prohibited this areaing from being saved:</h2>

  <ul>
    <% @areaing.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="card-body">
<div class="row">
  <div class="col-md-5">
    <div class="form-group">
          <%= f.label :article_area_id, class: "form-label" %>
      <%= f.number_field :article_area_id , class: "form-control" %>
          </div>
  </div>
</div>
<div class="row">
  <div class="col-md-5">
    <div class="form-group">
          <%= f.label :article_id, class: "form-label" %>
      <%= f.number_field :article_id , class: "form-control" %>
          </div>
  </div>
</div>
<div class="card-footer text-right">
  <button type="submit" class="btn btn-primary"><%=t :submit%></button>
</div>
<% end %>
