<div class="row row-cards">
  <div class="col-lg-3">
    <div class="row">
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-9">
    <div class="card">
      <table class="table card-table table-vcenter">
        <tbody>
          <% @article_formats.each do |article_format| %>
          <tr>
                        <td><%= article_format.title %></td>
                        <td><%= article_format.description %></td>
                        <td><%= link_to t(:show), article_format %></td>
            <td><%= link_to t(:edit), edit_article_format_path(article_format) %></td>
            <td><%= link_to t(:destroy), article_format, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
