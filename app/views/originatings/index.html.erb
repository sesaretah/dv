<div class="row row-cards">
  <div class="col-lg-3">
    <div class="row">
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-9">
    <div class="card">
      <table class="table card-table table-vcenter">
        <tbody>
          <% @originatings.each do |originating| %>
          <tr>
                        <td><%= originating.article_id %></td>
                        <td><%= originating.article_source_id %></td>
                        <td><%= link_to t(:show), originating %></td>
            <td><%= link_to t(:edit), edit_originating_path(originating) %></td>
            <td><%= link_to t(:destroy), originating, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
