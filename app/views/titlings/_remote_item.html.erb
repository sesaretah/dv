<div class="row">
  <div class="col-md-5">
    <div class="form-group">
      <% @r = SecureRandom.hex(8)%>
      <%= select_tag "#{@r}_title_type", options_for_select(TitleType.all.collect { |p| [ p.title, p.id ] }) , { :class => "comapct-form  form-control col-md-6"}%>
      <%= text_field_tag "#{@r}_other_title", nil , class: "form-control" %>
    </div>
  </div>
</div>
