<div class="row row-cards">
  <div class="col-lg-3">
    <div class="row">
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-9">
    <div class="card">
      <table class="table card-table table-vcenter">
        <tbody>
          <% @titlings.each do |titling| %>
          <tr>
                        <td><%= titling.title_type_id %></td>
                        <td><%= titling.article_id %></td>
                        <td><%= titling.status %></td>
                        <td><%= link_to t(:show), titling %></td>
            <td><%= link_to t(:edit), edit_titling_path(titling) %></td>
            <td><%= link_to t(:destroy), titling, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
