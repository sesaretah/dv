<div class="row row-cards">
  <div class="col-lg-3">
    <div class="row">
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-12">
        <div class="card">
          <div class="card-body">

          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-9">
    <div class="card">
      <table class="table card-table table-vcenter">
        <tbody>
          <% @formating_histories.each do |formating_history| %>
          <tr>
                        <td><%= formating_history.article_format_id %></td>
                        <td><%= formating_history.article_id %></td>
                        <td><%= formating_history.user_id %></td>
                        <td><%= formating_history.revision_number %></td>
                        <td><%= formating_history.workflow_state_id %></td>
                        <td><%= formating_history.formating_id %></td>
                        <td><%= link_to t(:show), formating_history %></td>
            <td><%= link_to t(:edit), edit_formating_history_path(formating_history) %></td>
            <td><%= link_to t(:destroy), formating_history, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
